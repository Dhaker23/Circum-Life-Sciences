import { NextRequest } from "next/server"; import { ok, fail, parseOrThrow } from "@/lib/api-envelope"; import { requirePermission } from "@/lib/auth-context"; import { CreateVsmEdgeSchema } from "@/modules/lean/domain"; import * as svc from "@/modules/lean/service";
export async function POST(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; const ctx = await requirePermission("lean.vsm.create"); const body = parseOrThrow(CreateVsmEdgeSchema, await req.json()); return ok(await svc.createVsmEdge(ctx, body)); } catch (e) { return fail(e); } }
