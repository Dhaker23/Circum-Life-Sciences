import { NextRequest } from "next/server"; import { ok, fail, parseOrThrow } from "@/lib/api-envelope"; import { requirePermission } from "@/lib/auth-context"; import { CreateVsmNodeSchema } from "@/modules/lean/domain"; import * as svc from "@/modules/lean/service";
export async function POST(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; const ctx = await requirePermission("lean.vsm.create"); const body = parseOrThrow(CreateVsmNodeSchema, await req.json()); if (body.vsmId !== id) { const { ValidationError } = await import("@/lib/errors"); throw new ValidationError("vsmId must match path"); } return ok(await svc.createVsmNode(ctx, body)); } catch (e) { return fail(e); } }
