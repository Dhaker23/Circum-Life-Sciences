import { NextRequest } from "next/server"; import { ok, fail, parseOrThrow } from "@/lib/api-envelope"; import { requirePermission } from "@/lib/auth-context"; import { CreateMonitoringPointSchema } from "@/modules/phase9/domain"; import * as svc from "@/modules/phase9/service";
export async function POST(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const ctx = await requirePermission("cleanroom.create"); const body = parseOrThrow(CreateMonitoringPointSchema, await req.json()); return ok(await svc.createMonitoringPoint(ctx, body)); } catch (e) { return fail(e); } }
