import { NextRequest } from "next/server"; import { ok, fail, parseOrThrow } from "@/lib/api-envelope"; import { requirePermission } from "@/lib/auth-context"; import { ExcursionTransitionSchema } from "@/modules/phase9/domain"; import * as svc from "@/modules/phase9/service";
export async function POST(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; const ctx = await requirePermission("cleanroom.excursion.transition"); const body = parseOrThrow(ExcursionTransitionSchema, await req.json()); return ok(await svc.transitionExcursion(ctx, id, body)); } catch (e) { return fail(e); } }
