import { NextRequest } from "next/server"; import { ok, fail, parseOrThrow } from "@/lib/api-envelope"; import { requirePermission } from "@/lib/auth-context"; import { SterTransitionSchema } from "@/modules/phase9/domain"; import * as svc from "@/modules/phase9/service";
export async function POST(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; const ctx = await requirePermission("sterilization.transition"); const body = parseOrThrow(SterTransitionSchema, await req.json()); return ok(await svc.transitionSterilization(ctx, id, body)); } catch (e) { return fail(e); } }
