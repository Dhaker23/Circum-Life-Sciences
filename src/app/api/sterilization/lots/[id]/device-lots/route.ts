import { NextRequest } from "next/server"; import { ok, fail, parseOrThrow } from "@/lib/api-envelope"; import { requirePermission } from "@/lib/auth-context"; import { LinkDeviceLotSchema } from "@/modules/phase9/domain"; import * as svc from "@/modules/phase9/service";
export async function POST(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; const ctx = await requirePermission("sterilization.create"); const body = parseOrThrow(LinkDeviceLotSchema, await req.json()); return ok(await svc.linkDeviceLot(ctx, id, body)); } catch (e) { return fail(e); } }
