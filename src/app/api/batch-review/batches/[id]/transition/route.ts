import { NextRequest } from "next/server"; import { ok, fail, parseOrThrow } from "@/lib/api-envelope"; import { requirePermission } from "@/lib/auth-context"; import { BatchReviewTransitionSchema } from "@/modules/phase9/domain"; import * as svc from "@/modules/phase9/service";
export async function POST(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; const ctx = await requirePermission("batchreview.transition"); const body = parseOrThrow(BatchReviewTransitionSchema, await req.json()); return ok(await svc.transitionBatchReview(ctx, id, body)); } catch (e) { return fail(e); } }
